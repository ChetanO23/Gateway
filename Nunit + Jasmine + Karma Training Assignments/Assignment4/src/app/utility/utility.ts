export class Utility {
    constructor() {}

    // Sum Method
    sumOperation(a:number, b:number) {
        const c = (a+b);
        return c;
    }
    
    // Average Method
    averageOperation(a:number, b:number, c:number) {
        const ans = (a+b+c)/3;
        return ans;
    }
}
